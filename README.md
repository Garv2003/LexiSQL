# LexiSQL

A natural-language-to-SQL tool — type a question in plain English and get the SQL query back.

## Features
- Translates natural-language prompts into SQL
- TypeScript codebase

## Run locally
```bash
npm install
npm run dev
```
